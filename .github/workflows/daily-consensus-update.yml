name: Daily Sharp Consensus Update

on:
  schedule:
    # Runs at 4:30 PM UTC (8:30 AM PST / 11:30 AM EST)
    - cron: '30 16 * * *'
  workflow_dispatch:  # Allows manual trigger

permissions:
  contents: write

jobs:
  update-consensus:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 lxml

      - name: Run Sharp Consensus Update
        run: |
          python scripts/github_consensus_update.py

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git diff --staged --quiet || git commit -m "Daily Sharp Consensus update - $(date +'%A, %B %d, %Y')

          🤖 Auto-generated by GitHub Actions"
          git push
